# docs

`screenshot.png` — the listings view, referenced from the top-level README.

Retake it against `make demo` with the browser at roughly 1400px wide, showing
the filter panel, a few `[DEMO ...]` cards with countdowns in different bands,
and a listing open in the detail panel.
